pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Example build command
                // For Python: sh 'python setup.py build'
                // For Node.js: sh 'npm install && npm run build'
                // For Java: sh 'mvn clean package'
            }
        }
    }
}
