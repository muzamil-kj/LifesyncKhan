npx ganache --db ganache-data --chain.chainId 1337 (Backend)
 npx hardhat run scripts/deploy.js --network ganache (Contracts)
 cd Lifesync/src/components/Backend

 improve the design and strructure of this section now... there should be proper colums that show Generated at , Day and appropriate columns for viwing and deleting report

<div className="past-reports-section">
        <h3 className="mn-head">
          📜 Your <span className="mn-high">Past</span> Reports
        </h3>
        <p className="mn-sub">Access and manage your previously generated reports.</p>
        <ul className="past-reports-list">
          {pastReports.length === 0 ? (
            <p>No past reports found.</p>
          ) : (
            pastReports.map((report) => (
              <li key={report._id} className="past-report-item">
                <span>
                  {new Date(report.createdAt).toLocaleString()} —{" "}
                  {new Date(report.createdAt).toLocaleDateString("en-US", { weekday: "long" })}
                </span>
                <div>
                  <button onClick={() => handleViewPastReport(report._id)}>View</button>
                  <button onClick={() => handleDeletePastReport(report._id)}>Delete</button>
                </div>
              </li>
            ))
          )}
        </ul>
      </div>

css:

/* ===== PAST REPORTS SECTION ===== */

.past-reports-section {
  background: #ffffff;
  padding: 20px;
  border-radius: 12px;
  box-shadow: 0 3px 6px rgba(0,0,0,0.05);
  margin-top: 20px;
}

.past-reports-list {
  list-style: none;
  padding: 0;
  margin-top: 10px;
}

.past-report-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: #f8f9fa;
  padding: 12px 16px;
  border-radius: 8px;
  margin-bottom: 8px;
}

.past-report-item span {
  font-size: 14px;
  color: #555;
}

.past-report-item button {
  margin-left: 8px;
  padding: 5px 10px;
  font-size: 13px;
  border: none;
  border-radius: 6px;
  cursor: pointer;
}

.past-report-item button:first-child {
  background: #4cafef;
  color: white;
}

.past-report-item button:last-child {
  background: #f44336;
  color: white;
}


